1. npm init -y // -y take all the default values
2. npx tsc --init // create a tsconfig.json file
3. tsc index.ts // compiles the ts file and creates a js file
we need to compile the typescript before being able to run it!
but then we have to type node index.js not .ts
4. npm i @types/node -D// -D means dev dependency